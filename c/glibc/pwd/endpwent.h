#include <sys/types.h>
#include <pwd.h>

struct passwd *getpwent(void); //get password file entry

void setpwent(void); //get password file entry

void endpwent(void); //get password file entry

